import React from "react";
import { StyleProp, ViewProps, ViewStyle } from "react-native";
export declare enum Bearing {
    Top = 0,
    Bottom = 1,
    Left = 2,
    Right = 3,
    Center = 4
}
declare type Props = ViewProps & {
    visible: boolean;
    style?: StyleProp<ViewStyle>;
    children?: React.ReactNode;
    entranceBearing?: Bearing;
    leaveBearing?: Bearing;
    fadeOutScale?: number;
    duration?: number;
    shouldEnterWithAnimation?: boolean;
    bearingMoveDistance?: number;
    removeChildrenAfterDisapearance?: boolean;
    animationFinished?: (isVisble: boolean) => void;
    [x: string]: any;
};
declare const FadeView: ({ style, visible, children, duration, fadeOutScale, shouldEnterWithAnimation, bearingMoveDistance, leaveBearing, entranceBearing, removeChildrenAfterDisapearance, animationFinished, ...rest }: Props) => JSX.Element;
export default FadeView;
